import {
} from 'actions/actionTypes';

const initialState = {};

export default function petitionsFilters (state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
