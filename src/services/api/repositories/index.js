import authRepository from './auth';
import citiesRepository from './city';
import petitionRepository from './petition';

export default {
  auth: authRepository,
  cities: citiesRepository,
  petition: petitionRepository
};
